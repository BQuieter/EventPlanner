<UserControl x:Class="EventPlannerClient.Views.CalendarEvents"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:viewModels="clr-namespace:EventPlannerClient.ViewModels"
             xmlns:local="clr-namespace:EventPlannerClient.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/ResourceDictionaries/Dark.xaml"/>
            </ResourceDictionary.MergedDictionaries>
            <DataTemplate DataType="{x:Type viewModels:EventViewViewModel}">
                <local:EventView/>
            </DataTemplate>
            <DataTemplate DataType="{x:Type viewModels:EventCreateViewModel}">
                <local:EventCreate/>
            </DataTemplate>
        </ResourceDictionary>
    </UserControl.Resources>
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="100"/>
            <ColumnDefinition Width="3*"/>
            <ColumnDefinition Width="100"/>
            <ColumnDefinition Width="3*"/>
        </Grid.ColumnDefinitions>
        <Viewbox Grid.Row="0" Grid.ColumnSpan="3" VerticalAlignment="Top">
            <Calendar SelectedDate="{Binding Date}" CalendarItemStyle="{DynamicResource CalendarCalendarItemStyle}" CalendarDayButtonStyle="{DynamicResource CalendarCalendarDayButtonStyle}" CalendarButtonStyle="{DynamicResource CalendarCalendarButtonStyle}" Style="{DynamicResource CalendarStyle}" />
        </Viewbox>

        <ListView Grid.Row="1" Grid.Column="1" ItemsSource="{Binding Events}" SelectedItem="{Binding SelectedEvent}" Margin="0 10 0 10" Style="{StaticResource CustomListViewStyle}" >
            <ListView.View>
                <GridView ColumnHeaderContainerStyle="{StaticResource CustomGridViewColumnHeaderStyle}">
                    <GridViewColumn Width="55" DisplayMemberBinding="{Binding Path=TimeString}">Время</GridViewColumn>
                    <GridViewColumn DisplayMemberBinding="{Binding Path=OwnerLogin}">Пользователь</GridViewColumn>
                    <GridViewColumn DisplayMemberBinding="{Binding Path=ImportanceString}">Важность</GridViewColumn>
                    <GridViewColumn DisplayMemberBinding="{Binding Path=Description}">Описание</GridViewColumn>
                </GridView>
            </ListView.View>
        </ListView>
        <Button Grid.Row="1" Grid.Column="0" Content="Обновить" Command="{Binding UpdateCommand}" Width="70" Style="{StaticResource ButtonStyle}"/>
        <Grid Grid.Row="1" Grid.Column="2">
            <Grid.RowDefinitions>
                <RowDefinition/>
                <RowDefinition/>
            </Grid.RowDefinitions>
            <Button Grid.Row="0" Content="Добавить" Command="{Binding AddCommand}" Width="70" Style="{StaticResource ButtonStyle}"/>
            <Button Grid.Row="1" Content="Удалить" Command="{Binding DeleteCommand}" Width="70" Style="{StaticResource ButtonStyle}"/>
        </Grid>
        <!--Просмотр/Создание/Редактирование-->
        <UserControl Grid.RowSpan="2" Grid.Column="3" Content="{Binding CurrentEventViewModel}"/>

    </Grid>
</UserControl>
