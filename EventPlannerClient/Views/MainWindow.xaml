<Window x:Class="EventPlannerClient.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:converters="clr-namespace:EventPlannerClient.Converters"
        xmlns:viewModels="clr-namespace:EventPlannerClient.ViewModels"
        xmlns:local="clr-namespace:EventPlannerClient.Views"
        mc:Ignorable="d"
        WindowStartupLocation="CenterScreen"
        AllowsTransparency="True"
        WindowStyle="None" d:DesignHeight="400" d:DesignWidth="800"
        Title="MainWindow" Style="{DynamicResource WindowStyle}" >
    <WindowChrome.WindowChrome>
        <WindowChrome ResizeBorderThickness="8" CaptionHeight="32"/>
    </WindowChrome.WindowChrome>
    
    <Window.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/ResourceDictionaries/Dark.xaml"/>
            </ResourceDictionary.MergedDictionaries>
            <converters:BoolToVisibilityConverterC x:Key="BoolToVisibility" IsReversed="False"/>
            <converters:BoolToVisibilityConverterC x:Key="BoolToVisibilityReversed" IsReversed="True"/>
            <DataTemplate DataType="{x:Type viewModels:CalendarEventsViewModel}">
                <local:CalendarEvents/>
            </DataTemplate>
            <DataTemplate DataType="{x:Type viewModels:AuthorizationViewModel}">
                <local:Authorization/>
            </DataTemplate>
            <DataTemplate DataType="{x:Type viewModels:RegistrationViewModel}">
                <local:Registration/>
            </DataTemplate>
        </ResourceDictionary>
        
    </Window.Resources>
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="32"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <!--Кастомный заголовок окна-->
        <Grid Grid.Row="0" Style="{StaticResource WindowHeaderStyle}">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition Width="32"/>
                <ColumnDefinition Width="32"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="0" Text="EventPlanner" VerticalAlignment="Center" Margin="10 0 0 0" Style="{StaticResource TextBlockStyle}"/>
            <Button Grid.Column="1" Content="-" WindowChrome.IsHitTestVisibleInChrome="True" Command="{Binding MinimizeCommand}" Style="{StaticResource MinimizeButtonStyle}"/>
            <Button Grid.Column="2" Content="X" WindowChrome.IsHitTestVisibleInChrome="True" Command="{Binding CloseCommand}" Style="{StaticResource CloseButtonStyle}"/>
        </Grid>

        <Grid Grid.Row="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="40"/>
                <RowDefinition/>
            </Grid.RowDefinitions>
            <!--Грид для отображение о пользователе (логин), возможность входа-->
            <Grid Grid.Row="0" Background="#252525">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="auto"/>
                    <ColumnDefinition Width="auto"/>
                    <ColumnDefinition Width="auto"/>
                    <ColumnDefinition Width="auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="{Binding IsAuthorizedString}" Margin="10 0 0 0" Style="{StaticResource TextBlockStyle}"/>
                <TextBlock Grid.Column="1" Text="{Binding UserLogin}" Margin="10 0 0 0" Style="{StaticResource TextBlockStyle}"/>
                <Button Grid.Column="2" Content="Войти" Margin="10 0 10 0" Command="{Binding AuthorizationCommand}" Style="{StaticResource UserAuthorizationButtonStyle}" />
                <Button Grid.Column="3" Content="Регистрация" Width="90" Margin="10 0 10 0" Command="{Binding RegistrationCommand}" Style="{StaticResource UserAuthorizationButtonStyle}" />

            </Grid>
            <Grid Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="2*"/>
                    <ColumnDefinition/>
                </Grid.ColumnDefinitions>
                <!--Календарь и все события-->
                <ContentControl Grid.Column="0" Content="{Binding CurrentMainViewModel}">
                    
                </ContentControl>
                <!--Изменение и создание-->
                <ContentControl Grid.Column="1">

                </ContentControl>
            </Grid>
        </Grid>
    </Grid>
</Window>
